# 次回タスク

> 更新日: 2026-03-28
> 次のセッションで着手すべき項目を優先度順に整理

---

## 最初にやること（セッション開始時の儀式）

```bash
# 1. TypeScript エラー確認
npx tsc --noEmit

# 2. Metro キャッシュクリア + 起動
npx expo start -c

# 3. iOS/Android 両方で動作確認
```

---

## 必須タスク（ブロッカー）

### 1. DBマイグレーション適用（2件）
- **ファイル1**: `supabase/migrations/20260328000000_ghost_badges_history.sql`
- **ファイル2**: `supabase/migrations/20260328000001_search_posts_trgm.sql`
- **方法**: Supabase Dashboard → SQL Editor → New query → 貼り付け → Run
- **影響**:
  - ファイル1: ゴースト投稿・バッジ・街の記憶が動作しない
  - ファイル2: 全文検索（pg_trgm）が動作しない（ILIKEフォールバックあり）
- **工数**: 5分

---

## 優先タスク（着手推奨順）

### 2. サーバーフィルタ + ページネーション強化
- **現状**: フィード一覧は `useInfiniteQuery` 導入済み、ちかくは一括取得
- **やること**:
  - ちかく画面のページネーション検討
  - サーバーサイドでの期限切れ投稿除外（WHERE句追加のみ）
- **工数**: M（半日）

### 3. スケルトンスクリーン
- **現状**: ローディング中は `StateView` のスピナーのみ
- **やること**:
  - ホーム画面（ちかく/フィード両モード）のスケルトン
  - トーク画面のスケルトン
- **工数**: M（半日）

### 4. プッシュ通知設計
- **現状**: 未着手
- **やること**:
  - expo-notifications の導入設計
  - 通知トリガーの定義（いいね、返信、近くの新規投稿）
  - 設計書作成が先
- **工数**: S（設計のみ1時間）

### 5. リリース準備
- **やること**:
  - EAS Build 設定
  - bundleIdentifier 確定
  - プライバシーポリシー作成
- **工数**: L（1日）

---

## 確認ポイント（レビュー観点）

次のセッションで以下を実機確認:

- [ ] ホーム画面: ちかく⇔フィードのモード切替が正常に動作するか
- [ ] ホーム画面: フィードモードで日付・カテゴリフィルタが動作するか
- [ ] ヘッダー: ログイン時にアバターが表示されるか（24px丸）
- [ ] ヘッダー: ゲスト時にUserアイコン+「ログイン」が表示されるか
- [ ] ヘッダー: アバタータップでプロフィール編集、ゲスト時はログイン画面へ遷移するか
- [ ] 検索: pg_trgmマイグレーション適用後、検索結果が類似度順に並ぶか
- [ ] ゴースト投稿: 24h以上前のトークが非表示になるか（DB適用後）
- [ ] 殿堂入り: いいね10件で金バッジが出るか
- [ ] 街の記憶: ちかく画面→ボタン→月別一覧が表示されるか
- [ ] バッジ: プロフィール画面にバッジセクションが出るか
- [ ] 検索: トーク検索タブが動作するか
- [ ] iOS: 設定画面が正常に開くか
- [ ] iOS: トーク投稿→投稿完了→自動で戻るか（破棄確認が出ないか）

---

## 完了済み（前回セッション）

| タスク | 内容 |
|--------|------|
| ~~DBマイグレーション作成~~ | `20260328000000` + `20260328000001` 作成済み（適用は手動） |
| ~~タブ統合（5→4）~~ | ホーム画面にちかく/フィードのモード切替を統合 |
| ~~ログイン状態の常時表示~~ | Header.tsxにアバター/ゲストログイン導線を追加 |
| ~~全文検索への移行~~ | pg_trgm RPC関数 + lib/posts.ts を更新 |
| ~~ブックマーク~~ | 不要と判断、タスクから除外 |
